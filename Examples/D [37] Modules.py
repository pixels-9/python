# module = a file containing Python definitions and statements

# import [] = import a module
# import [] as {} = import a module and give it a nickname (alias)
# from [] import {} = import a specific name from the module (can be a function, class or variable)
# from [] import * = import all names from the module (dangerous as can overwrite names)

# from = keyword used to import specific names from a module (no need for the module name when using the function)

help("modules")
